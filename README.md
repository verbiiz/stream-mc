# stream-mc

Minecraft Forge mod (1.20.1).

## Setup

1. Install JDK 17.
2. Clone this repo.
3. Run `./gradlew runClient` (Windows: `gradlew.bat runClient`).

The mod prints `stream-mc loaded` to the console on startup.
